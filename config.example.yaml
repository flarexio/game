webrtc:
  iceServers:
  - provider: google
  - provider: cloudflare
    id: ...
    token: ...
  - provider: metered
    id: ...
    token: ...

streams:
- name: gamestream
  transport: nvstream
  address: https://localhost:47984
  video: 
    codec: h264
  audio: 
    codec: opus
- name: stream
  transport: raw
  video:
    codec: h264
    address: unix:///tmp/stream/video.sock
  audio:
    codec: opus
    address: unix:///tmp/stream/audio.sock
